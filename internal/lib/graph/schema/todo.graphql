type Todo {
  id: ID!
  text: String!
  done: Boolean!
  user: User!
}

extend type Query {
  todoesByUserId(userId: ID!): [Todo!]!
}

input TodoCreateInput {
  text: String!
  userId: ID!
}

extend type Mutation {
  createTodo(input: TodoCreateInput!): Todo!
}
