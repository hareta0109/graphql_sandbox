type User {
  id: ID!
  name: String!
  department: Department!
}

extend type Query {
  user(id:ID!): User!
  users: [User!]!
}

input UserCreateInput {
  name: String!
  departmentId: ID!
}

extend type Mutation {
  createUser(input: UserCreateInput!): User!
}
